#! /usr/bin/python3

import time

from statusNano import main
from variableDictionary import varDict
from reportNano import reportNano

import sys

# the following loops between homeLeft and homeRight

# print out all 4 bytes of status string but move for S_MOVE = 0

def backAndForth (argv):
    if argv:

    else:
        
    if commNano != None:
        time.sleep(1)
        backAndForth(1)

    if argv:
        if reportNano("Status") = None:   # replace with a more robust condition (i.e. check the output of reportNano)
            backAndForth()

        else

    return
